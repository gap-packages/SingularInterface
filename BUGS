gap> Singular("print(a)");

runs into an error and Singular reads from STDIN.
return(); does not leave the interpreter but produces an error
After ^D we back at the GAP level
Subsequent Singular(""); commands produce segfault


