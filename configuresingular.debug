#!/bin/sh -ex

if [ ! -x singular/src/configure ] ; then
    (cd singular/src && ./autogen.sh)
fi

PREFIX=$PWD/singular/dst

mkdir -p $PREFIX
mkdir -p singular/build-debug
cd singular/build-debug
../src/configure --prefix=$PREFIX --enable-debug --disable-optimizationflags $@
